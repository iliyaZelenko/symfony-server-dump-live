import App from '~/App';
export default class WebSocket {
    private io;
    private app;
    constructor(app: App);
    start(): void;
    private listen;
    private getFileContent;
    private generateInitialHtml;
}
